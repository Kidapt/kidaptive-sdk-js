'use strict';

const defaultState = {
  apiKey: 'testApiKey',
  initialized: true,
  options: {
    tier: 1,
    authMode: 'client',
    environment: 'dev',
    autoFlushInterval: 0,
    loggingLevel: 'none'
  }
}

export default {
  defaultState
}
