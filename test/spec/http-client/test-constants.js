'use strict';

const customBaseUrl = 'http://testBase/';

const defaultApiKey = 'testApiKey';

const defaultEndpoint = 'testEndpoint';

const getMethod = 'GET';

const postMethod = 'POST';

const userApiKey = 'userApiKey';

export default {
  defaultApiKey,
  defaultEndpoint,
  getMethod,
  postMethod,
  userApiKey
}
